name: sports_app1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # ✅ 한글 입력을 위한 패키지 추가
    sdk: flutter         # ✅ 한글 입력을 위한 패키지 추가

  flutter_dotenv: ^5.1.0
  http: ^1.1.0
  url_launcher: ^6.2.1
  intl: ^0.20.2
  cupertino_icons: ^1.0.2
  google_maps_flutter: ^2.14.0
  geolocator: ^14.0.2
  supabase_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
